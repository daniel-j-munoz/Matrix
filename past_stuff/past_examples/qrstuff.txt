#include <opencv2/opencv.hpp>
#include "Libraries/Image.h"
#include "Libraries/Matrix.h"
#include <iostream>

using namespace std;
using namespace cv;

#include "Libraries/Eigen.h"

// Numerical Class here?....

// aitkens?...

#include "Factor.h"

int main(){


    // vector<float> data = {
    //     1.0f, 3.0f, 3.0f,
    //     3.0f, 5.0f, 7.0f,
    //     3.0f, 7.0f, 6.0f,
    //     4.0f, 1.0f, 7.0f,
    //     9.0f, 2.0f, 4.0f,
    // };

    // Matrix A(5, 3, data);

    // Symmetric       
    vector<float> data = {
        1.0f, 4.0f, 2.0f, 7.0f, 5.0f,
        4.0f, 3.0f, 5.0f, 6.0f, 8.0f,
        2.0f, 5.0f, 6.0f, 1.0f, 4.0f,
        7.0f, 6.0f, 1.0f, 9.0f, 3.0f,
        5.0f, 8.0f, 4.0f, 3.0f, 7.0f
    };

    Matrix A(5, 5, data);


    // Oberservations... 
    // So it seems when the input is a symmetric matrix we do get a triadiagonal form when we call triadonalize. 
    // but when the input is not triadogonal, it seems that we get an upper hessenberg matrix. 


    
    // Matrix eigen = Eigen::QR(A);


    // Matrix b(3, 1);
    // b.randomize(-10.0f, 10.0f);
    // Matrix eigenvector = Eigen::power(A, b);
    // eigenvector.print(2);



    // vector<Matrix> QR = Factor::QRHH(A);
    // QR.at(0).print(2);
    // cout << "\n";
    // QR.at(1).print(2);


    // cout << "\n";
    // (QR.at(0) * QR.at(1)).print(2);




    A = A.upper_hessenberg();
    A.print(2);
    cout << "\n";

    Matrix QR = Eigen::QR(A);




    return 0;
}




// Min(m - 1, n) iterations? hhoulder method?...
// shifted qr algo?


